// adjust this in case TypeScript versions between the app and the schematics don't match
// import * as typescript from 'typescript';
import * as typescript from '@schematics/angular/node_modules/typescript';

export = typescript;
