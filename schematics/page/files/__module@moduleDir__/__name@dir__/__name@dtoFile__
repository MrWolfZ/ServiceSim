import { Ask } from '../../infrastructure/infrastructure.dto';

export interface <%= dto(name) %> {

}

export const <%= askKindConstant(name) %> = '<%= dir(path) %>/<%= dir(name) %>/ASK_FOR_PAGE_DTO';

export interface <%= askInterface(name) %> extends Ask<typeof <%= askKindConstant(name) %>, <%= dto(name) %>> { }

export const <%= askConstant(name) %>: <%= askInterface(name) %> = {
  kind: <%= askKindConstant(name) %>,
};
